{
  "mcpServers": {
    "filesystem": {
      "description": "Read-only filesystem access for inspecting repo structure, configs, logs",
      "transport": "stdio",
      "command": "hermes-mcp-filesystem",
      "args": ["--read-only", "--allowed-paths", "/apps"]
    },
    "git": {
      "description": "Read-only git access for viewing branch, log, diff, status",
      "transport": "stdio",
      "command": "hermes-mcp-git",
      "args": ["--read-only", "--allowed-paths", "/apps/repos"]
    },
    "docker": {
      "description": "Read-only docker access for ps, inspect, logs, stats",
      "transport": "stdio",
      "command": "hermes-mcp-docker",
      "args": ["--read-only"]
    },
    "terminal": {
      "description": "Terminal access for diagnostic commands, system inspection",
      "transport": "stdio",
      "command": "hermes-mcp-terminal",
      "args": ["--read-only", "--filter-commands"]
    },
    "telegram": {
      "description": "Primary communication gateway",
      "transport": "stdio",
      "command": "hermes-mcp-telegram",
      "args": []
    }
  }
}
